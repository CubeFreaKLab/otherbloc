# Release-controller configuration only; never source this into an application.
# Keep real values in the protected production environment, not Git or chat.
# APP_REVISION is for read-only local preflight; Actions supplies GITHUB_SHA.
APP_REVISION=
RENDER_API_KEY=
RENDER_BLUEPRINT_ID=
RENDER_USERS_SERVICE_ID=
RENDER_USERS_ORIGIN=
RENDER_CONTENT_SERVICE_ID=
RENDER_CONTENT_ORIGIN=
RENDER_INTERACTIONS_SERVICE_ID=
RENDER_INTERACTIONS_ORIGIN=
RENDER_GATEWAY_SERVICE_ID=
RENDER_GATEWAY_ORIGIN=
